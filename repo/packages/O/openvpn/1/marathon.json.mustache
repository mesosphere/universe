{
  "id": "{{openvpn.framework-name}}",
  "cpus": {{openvpn.cpus}},
  "mem": {{openvpn.mem}},
  "secrets": [ {
    "secret0": {
      "source": "ovpn_username"
    },
    "secret1": { 
      "source": "ovpn_password"
    }
  } 
  ],
  "instances": {{openvpn.instances}},
  "acceptedResourceRoles": ["slave_public"],
  "constraints": [
    ["hostname", "UNIQUE"]
  ],
  "container": {
    "type": "DOCKER",
    "docker": {
      "image": "{{resource.assets.container.docker.openvpn-docker}}",
      "forcePullImage": true,
      "network": "BRIDGE",
      "privileged": true,
      "portMappings": [
        { "containerPort": 5000, "protocol": "tcp" },
        { "containerPort": 1194, "protocol": "udp" }
      ]
    }
  },
  "healthChecks": [
    {
      "gracePeriodSeconds": 120,
      "intervalSeconds": 30,
      "timeoutSeconds": 5,
      "maxConsecutiveFailures": 3,
      "path": "/status",
      "portIndex": 0,
      "protocol": "MESOS_HTTPS",
      "ignoreHttp1xx": false
    }
  ],
  "env": {
    "MESOS_CONFIG": "zk://master.mesos:2181/mesos",
    "FRAMEWORK_NAME": "{{openvpn.framework-name}}",
    "OVPN_USERNAME": "{{openvpn.secret0}}",
    "OVPN_PASSWORD": "{{openvpn.secret1}}"
  },
  "labels": {
    "DCOS_PACKAGE_FRAMEWORK_NAME": "{{openvpn.framework-name}}"
  }
}