{
  "type": "object",
  "properties": {
    "artifactory-primary": {
      "description": "Artifactory framework configuration properties",
      "type": "object",
      "properties": {
        "framework-name": {
          "description": "The framework name to register with Mesos.",
          "type": "string",
          "default": "artifactory-primary"
        },
        "cpus": {
          "description": "CPU shares to allocate to Artifactory-Primary instance.",
          "type": "number",
          "default": 2.0,
          "minimum": 1.0
        },
        "mem": {
          "description": "Memory to allocate to Artifactory-Primary instance.",
          "type": "number",
          "default": 2048.0,
          "minimum": 1024.0
        },
        "unique-nodes": {
          "description": "Run nodes on unique hosts.",
          "type": "boolean",
          "default": true
        },
        "cluster-home": {
          "description": "The location of a volume on the host to be used for persisting cluster home directory used when running Artifactory in HA mode. The final location will be derived from this value plus the name set in `framework-name` (e.g. `/mnt/host_volume/artifactory`). Note that this path must be the same on all DC/OS agents.",
          "type": "string"
        },
        "database": {
          "description": "High availability database properties.",
          "type": "object",
          "properties": {
            "database-connection-string": {
              "description": "Database connection string.",
              "type": "string"
            },
            "database-user": {
              "description": "Database username.",
              "type": "string"
            },
            "database-password": {
              "description": "Database password.",
              "type": "string"
            }
          }
        },
        "licenses": {
          "description": "Provide Licenses comma separated to be used by Artifactory instances",
          "type": "string",
          "default": "NONE"
        }
      },
      "required": [
        "framework-name",
        "cpus",
        "mem",
        "cluster-home"
      ]
    }
  },
  "required": [
    "artifactory-primary"
  ]
}